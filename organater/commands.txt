cd organater
source ../.venv/bin/activate
cd python_scripts/


# 1. カスタムモジュールへのパスを設定
# nest_module/build に libnest_module.so が存在することを前提とします。
export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:$PWD/nest_module/build

# 2. Pythonスクリプトの実行
python simulation.py




# 1. ビルドディレクトリに移動
cd ~/data/organater/nest_module/build

# 2. CMAKE_PREFIX_PATHを指定してcmakeを実行
cmake -DCMAKE_PREFIX_PATH=/home/s24156/data/.venv ..

# 3. コンパイル
make


# Git操作

git status

git add .

git commit -m "changed"

git push origin main